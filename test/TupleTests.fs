module Tests

open Xunit

[<Fact>]
let tuple_point_behaviour () =
    let x, y, z = 4.3, -4.2, 3.1
    // let a = tuple x y z 1.0
    // Assert.Equal(x, Tuple.X a)
    // Assert.Equal(y, Tuple.Y a)
    // Assert.Equal(z, Tuple.Z a)
    // Assert.True(isPoint a)
    // Assert.False(isVector a)
    ()